import { Typography } from "@mui/material";
import { NextPage } from "next";
import MainContainer from "../../components/MainContainer";

const Pedidos: NextPage = () => {
  return (
    <MainContainer>
      <Typography variant="title1">Pedidos</Typography>
    </MainContainer>
  );
};

export default Pedidos;
